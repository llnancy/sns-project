package org.lilu.sns.util;

import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

/**
 * @Auther: lilu
 * @Date: 2019/2/21
 * @Description:
 */
@RunWith(SpringRunner.class)
@SpringBootTest
public class RedisAdapterTest {
//    @Autowired
//    private RedisClientAdapter redisClient;
//
//    @Test
//    public void test() {
//        RedisClusterCommands<String,String> redis = redisClient.getRedisClusterClient();
//        System.out.println(redis);
//        // JedisCluster jedisCluster = (JedisCluster) redis.getConnectionFactory().getClusterConnection().getNativeConnection();
//        redis.bzpopmin(0,RedisKeyUtil.getEventSetKey());
//    }
}